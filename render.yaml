services:
  - type: web
    env: node
    # Static site name on Render: kua-center
    # API service name on Render: kua-center-api
    name: kua-center-api
    branch: main
    buildCommand: npm run ci:validate
    startCommand: npm start
    autoDeployTrigger: commit
    plan: starter
    envVars:
      - key: KUAC_DATA_DIR
        value: /opt/render/project/src/data
    disk:
      name: kuac-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1

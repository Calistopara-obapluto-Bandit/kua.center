# Public static site: no runtime variables are needed.
# The private dashboard gate now uses the browser config in config.js.
# Optional: override the server data directory when running locally.
# KUAC_DATA_DIR=./data

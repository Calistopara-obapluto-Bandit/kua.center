services:
  - type: web
    runtime: node
    name: kua-center
    branch: main
    buildCommand: echo "Static site and API ready"
    startCommand: node server.js
    autoDeployTrigger: commit
    plan: free

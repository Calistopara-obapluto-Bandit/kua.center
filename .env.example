# Public site: no runtime variables are needed.
# Private KUAC admin session access code:
AGENT_DASHBOARD_PASSWORD=change-me

# Replace the placeholder with the real admin code in your local .env file.
